# btsync
# Made by puppet
#
description	"btsync"

start on runlevel [2345]
stop on runlevel [!2345]

respawn

exec su <%= scope.lookupvar('btsync::user') %> -c "btsync --nodaemon --config /etc/btsync.conf"
